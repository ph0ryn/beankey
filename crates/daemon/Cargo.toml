[package]
name = "beankey-daemon"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
beankey-converter = { path = "../converter" }
beankey-llama = { path = "../llama" }
libc = "0.2.189"
prost.workspace = true
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.149"
toml = "1.1.4"

[build-dependencies]
prost-build = "0.14.1"

[dev-dependencies]
tempfile = "3.27.0"
